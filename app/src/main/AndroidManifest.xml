<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_image_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_image_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.SanitariuszApp"
        tools:targetApi="31">
        <activity android:name=".PatientDetailsActivity"
            android:theme="@style/Theme.SanitariuszApp"/>
        <activity android:name=".EditProcedureActivity"
            android:theme="@style/Theme.SanitariuszApp"/>
        <activity android:name=".activities.Afternoon"
            android:theme="@style/Theme.SanitariuszApp"/>
        <activity android:name=".activities.Contact"
            android:theme="@style/Theme.SanitariuszApp"/>
        <activity android:name=".activities.Archive"
            android:theme="@style/Theme.SanitariuszApp"/>
        <activity android:name=".activities.ToTake"
            android:theme="@style/Theme.SanitariuszApp"/>
        <activity android:name=".AddToAfternoonActivity"
            android:theme="@style/Theme.SanitariuszApp"/>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>