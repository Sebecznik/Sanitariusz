<!-- res/values/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Podstawowy motyw aplikacji -->
    <style name="Theme.SanitariuszApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Pozwala rysować pod paskiem statusu -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- Ustawiamy przezroczysty status bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <item name="android:fontFamily">@font/secondary_font</item>
    </style>
</resources>
